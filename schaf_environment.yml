name: final_schaf
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.9
  - numpy
  - pandas
  - scanpy
  - anndata
  - pytorch
  - torchvision
  - cudatoolkit=11.8  # For GPU support, adjust if your GPU needs a different version
  - scikit-learn
  - tqdm
  - matplotlib
  - seaborn
  - pillow
  - imageio
  - opencv
  - numba
  - scipy
  - pip
  - pip:
      - wandb
      - tangram-sc
      - scgpt
      - timm
      - einops
